"""
URLs for ibl_app.
"""
from django.urls import re_path  # pylint: disable=unused-import
from django.views.generic import TemplateView  # pylint: disable=unused-import

urlpatterns = [
    
    # re_path(r'', TemplateView.as_view(template_name="ibl_app/base.html")),
]
